---
label: Hero
hide_body: false
fields:
- name: hero
  type: field_group
  config: {}
  fields:
  - name: heading
    type: text
    config:
      required: false
    label: Heading
  - name: subtitle
    type: text
    config:
      required: false
    label: Subtitle
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - name: alt_text
    type: text
    config:
      required: false
      min: 8
    label: Image Alt Text
    description: Alternate text in case image can't be displayed.
  label: Hero
  description: A banner image with optional text overlay
