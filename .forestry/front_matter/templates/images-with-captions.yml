---
label: Images with captions
hide_body: true
fields:
- name: images
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - name: alt_text
    type: text
    config:
      required: true
    label: Alt text
    description: Required for accessibility.
  - name: caption
    type: text
    config:
      required: false
    label: Caption
  config:
    min: 
    max: 
    labelField: 
  label: Images
